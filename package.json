{
  "name": "ranger-ims-go",
  "private": true,
  "type": "module",
  "scripts": {
    "fixtures": "go run bin/rendertestfixtures/rendertestfixtures.go",
    "pretest": "npm run fixtures && npm run typecheck",
    "test": "vitest run",
    "pretest:watch": "npm run fixtures",
    "test:watch": "vitest",
    "pretest:coverage": "npm run fixtures",
    "test:coverage": "vitest run --coverage",
    "typecheck": "go tool tsc -p web/typescripttest --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "@vitest/coverage-v8": "^5.0.1",
    "happy-dom": "^20.14.5",
    "vitest": "^5.0.1"
  }
}
